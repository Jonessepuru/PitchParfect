# PitchPerfect Android

Setup:
1. Firebase console -> new project
2. Enable Auth Email/Password
3. Firestore + Storage + FCM
4. Download google-services.json to app/
5. Collections: users, projects, matches, messages, tasks, mentors, events

Build: ./gradlew assembleDebug
